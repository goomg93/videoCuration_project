import React from 'react';
import { BrowserRouter, Routes, Route } from 'react-router-dom';
function Router() {
  <BrowserRouter>
    <Routes>
      <Route></Route>
    </Routes>
  </BrowserRouter>;
}

export default Router;
